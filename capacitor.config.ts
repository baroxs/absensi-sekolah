import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mtselyaqien.absensi',
  appName: 'Absen Mts Plus Elyaqien',
  webDir: 'dist'
};

export default config;
